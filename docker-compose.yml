services:
  app:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: team-chat-app
    volumes:
      - ./backend:/var/www/html
    depends_on:
      - postgres
    networks:
      - team-chat

  nginx:
    image: nginx:alpine
    container_name: team-chat-nginx
    ports:
      - "8000:80"
    volumes:
      - ./backend:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - team-chat

  postgres:
    image: postgres:16
    container_name: team-chat-postgres
    environment:
      POSTGRES_DB: team_chat
      POSTGRES_USER: laravel
      POSTGRES_PASSWORD: laravel
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U laravel -d team_chat"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - team-chat

  pgadmin:
    image: dpage/pgadmin4
    container_name: team-chat-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@team-chat.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "8081:80"
    depends_on:
      - postgres
    networks:
      - team-chat

  reverb:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: team-chat-reverb
    command: php artisan reverb:start --host=0.0.0.0
    volumes:
      - ./backend:/var/www/html
    ports:
      - "8080:8080"
    depends_on:
      - app
    networks:
      - team-chat

networks:
  team-chat:
    driver: bridge

volumes:
  postgres-data: